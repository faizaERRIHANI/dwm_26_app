import 'package:flutter/material.dart';
class AnimatedGraphicsPage extends StatelessWidget {
  const AnimatedGraphicsPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Animation')),
      body: const Center(child: Text('Animation Page')));
  }
}
